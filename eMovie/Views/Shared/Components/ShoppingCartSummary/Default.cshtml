@model int;

@if (User.Identity.IsAuthenticated && Model > 0) 
{
	<li class="nav-item unstyled">
		<a class="nav-link text-dark" asp-controller="Orders" asp-action="ShoppingCart">
			<i class="bi bi-cart4"></i>@Model</a>
	</li>
}